Comparing the live result against the reference mockup (image 20)
directly, section by section. These are the specific gaps - close these,
don't do a general re-pass over everything.

1. Header: confirm the full title ("The Mafia of Hollow Creek" +
   tagline), the "Hollow Creek Est. 1884" hanging sign, and the lamppost
   are actually present above the currently-visible area - if they're
   missing entirely rather than just scrolled above view, they need to
   be built.

2. Town roster (left column): every player's name label is missing
   under their portrait - this is the clearest, most obvious gap. Also
   missing: "The Town" section header and a subtitle line (reference
   uses "10 souls. One of them deceives.").

3. Town Bulletin (center column): currently a single collapsed line.
   The reference builds this as three separate note-cards side by side
   (Recent News / Phase Instructions / Player Count) plus a distinct
   Public Announcement banner beneath them - build it as that structure,
   not one line of text.

4. The Stage: structurally close and working. Missing specifically: the
   thumbs-up / thumbs-down / smiley reaction row beneath the speaker
   portrait.

5. Voting/ballot box: the entire "Your Vote" section from Task A doesn't
   appear anywhere in the current screenshot. Confirm directly whether
   it was actually built and is just positioned lower on the page, or
   wasn't built - these need different fixes.

6. Role envelope (right column): currently reads as a small,
   underdeveloped icon. Confirm this is actually rendering
   role-envelope-states.png at a real, visible size matching the
   reference, not a placeholder or a scaled-down icon version.

7. Bottom nav: Map is correctly excluded - leave it that way. But
   confirm Case Files is actually present as its own icon - only four
   are visible currently versus five expected (Case Files, Characters,
   Notes, History, Settings). Also add the desk-prop background details
   around the nav row (stacked books, lantern, hat) that the reference
   uses to ground that strip visually.

Go through these seven items in order, confirm each one specifically
before moving to the next - same approach as the build-order document,
since checking one thing at a time is what actually caught problems last
round.
